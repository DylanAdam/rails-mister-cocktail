<div class="container">
<%= simple_form_for @cocktail do |f| %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.button :submit,  "Change the picture", class: "btn-form" %>
<% end %>
</div>